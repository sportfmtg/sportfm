{
  "name": "sportfm",
  "private": true,
  "description": "SportFM website (sportfmtg.com) — index.html is the source; build.js precompiles it into dist/ for Vercel.",
  "scripts": {
    "build": "node build.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
